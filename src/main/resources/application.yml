server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: medcore

  profiles:
    active: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/health_system_db
    username: postgres
    password: ${DB_PASSWORD:imvfbh5cs}
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 30000
      connection-timeout: 20000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false

  jackson:
    time-zone: America/Lima

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace

security:
  jwt:
    # En producción DEBE definirse JWT_SECRET (>= 32 caracteres / 256 bits).
    # El default sólo aplica en desarrollo local.
    secret: ${JWT_SECRET:dev-only-medcore-secret-key-change-me-in-prod-0123456789}
    expiration: 900000
    refresh-expiration: 604800000